@model IEnumerable<DropIt.Models.Request>
@{
    ViewBag.Title = "ListRequest";
    Layout = "~/Areas/Administration/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="table-wrapper events-table section">
    <div class="row-fluid head">
        <div class="span12">
            <h4>Requests</h4>
        </div>
    </div>
    @* <div class="row-fluid filter-block">
        <div class="pull-right">
            <div class="ui-select">
                <select>
                    <option>Filter events</option>
                    <option>abc</option>
                    <option>xyz</option>
                </select>
            </div>
            <input type="text" class="search">
            <a class="btn-flat success new-event" href="Create">+ Add event</a>
        </div>
    </div>*@
    <div id="pad-wrapper">
        <div class="row-fluid">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th class="span3">
                            <span class="line"></span>người đăng
                        </th>
                        <th class="span3">
                            <span class="line"></span>tên sự kiện
                        </th>
                        <th class="span3">
                            <span class="line"></span>miêu tả yêu cầu
                        </th>
                        <th class="span3">
                            <span class="line"></span>tình trạng yêu cầu
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <!-- row -->
                    @foreach (var item in Model)
                    {
                        <tr class="first">
                            <td>
                                @Html.DisplayFor(modelItem => item.User.UserName)
                            </td>
                            <td class="">
                                @Html.DisplayFor(modelItem => item.Event.EventName)
                            </td>
                            <td class="description">
                                @Html.DisplayFor(modelItem => item.Description)
                            </td>
                            <td>
                                     @{
                                         var status = item.Status;
                                         if (status==0)
                                         {
                                             <p>đã đăng</p>
                                         }
                                         else 
                                         {
                                         <p>đã đóng</p>                    
                                            }
                                        
                                     }                      
                            </td>
                                
                           @* <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.TicketId }) |
                                @Html.ActionLink("Details", "Details", new { id = item.TicketId }) |
                                @Html.ActionLink("Delete", "Delete","Ticket" , new { id = item.TicketId }, new { onclick="return confirm('Bạn có chắc chắn muốn xóa vé này?');"})
                            </td>*@
                            <td>
                                @Html.ActionLink("Details", "Details", new { userid = item.UserId, id = item.EventId })|
                                @Html.ActionLink("Delete", "Delete","Request" , new { userid = item.UserId, id = item.EventId }, new { onclick="return confirm('Bạn có chắc chắn muốn xóa vé này?');"})

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <!-- end events table -->
</div>

@*<script>
    function confirm() {
        if (confirm("OK?") == true) {
            console.log("YES");
        }
    }
    
</script>*@
